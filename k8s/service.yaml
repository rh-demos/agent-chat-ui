apiVersion: v1
kind: Service
metadata:
  name: agent-chat-ui
  labels:
    app: agent-chat-ui
    app.kubernetes.io/name: agent-chat-ui
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: agent-chat-ui
